export enum Permissions {
  List = 'user.list',
  Read = 'user.read',
  Create = 'user.create',
  Update = 'user.update',
  Delete = 'user.delete',
}

export enum Events {
  AVATAR_UPLOAD = 'user.avatar.upload',
  FILE_UPLOAD = 'user.file.upload',
}
